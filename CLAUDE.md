## 개발 워크플로우

### TDD
1. spec 테스트(*.spec.test.tsx)로 수용 기준 정의 (Red)
2. 구현 테스트(*.test.tsx)로 순수 로직 단위 테스트 작성 (Red)
3. 테스트를 통과하는 최소 코드 구현 (Green)
4. 양쪽 테스트 통과를 유지하며 리팩터링

### 테스트 파일 컨벤션

| 파일 패턴 | 용도 |
|---|---|
| `*.spec.test.tsx` | 수용 기준 테스트 (spec에서 도출) |
| `*.test.tsx` | 구현 테스트 (단위/통합) |
